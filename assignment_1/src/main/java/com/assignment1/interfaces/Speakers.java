package com.assignment1.interfaces;

// we cannot create beans from interfaces. we can only create beans from java classes, so we don't use @ here
public interface Speakers {
    public String makeSound();
}
